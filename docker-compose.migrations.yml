version: '3.4'

services:
  migrations:
    build:
      context: ../../
      dockerfile: templates/service/database/Dockerfile
    depends_on:
      - sqlserver
    environment:
      - IdentityServer_DB=MessageKit_Identity
      - MessageKit_DB=MessageKit_ModuleDb
      - SA_PASSWORD=yourStrong(!)Password
